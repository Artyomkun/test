{
    "name": "calculator-ts",
    "version": "1.0.0",
    "scripts": {
        "test": "jest",
        "test:coverage": "jest --coverage --maxWorkers=1",
        "test:coverage:win": "set NODE_OPTIONS=--max-old-space-size=4096&& jest --coverage --maxWorkers=1",
        "test:watch": "jest --watch",
        "test:verbose": "jest --verbose"
    },
    "devDependencies": {
        "@types/jest": "^29.5.8",
        "jest": "^29.7.0",
        "ts-jest": "^29.1.1",
        "ts-node": "^10.9.1",
        "typescript": "^5.3.3"
    }
}